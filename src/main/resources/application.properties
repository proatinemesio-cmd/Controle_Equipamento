# Se existir uma variável de ambiente na nuvem, ele usa ela. Senão, usa o localhost para você testar no PC.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/controle_equipamentos}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:912293@}

# Configurações do JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect